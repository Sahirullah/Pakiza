{
  "buildCommand": "cd portfolio && npm install && npm run build",
  "outputDirectory": "portfolio/dist",
  "framework": "vite",
  "regions": ["iad1"],
  "env": [],
  "functions": {
    "api/**/*.js": {
      "memory": 1024,
      "maxDuration": 60
    }
  }
}
